# Direct dependencies, loose. This file is for humans and for deliberate upgrades:
#   pip install -r requirements.in && pip freeze > requirements.txt   (then run the tests)
# Streamlit Cloud installs requirements.txt, which pins every package exactly.
streamlit>=1.58
openai>=2.0
pydantic>=2.9
pillow>=11.0
google-api-python-client>=2.140
google-auth-oauthlib>=1.2
oauthlib>=3.3
requests>=2.34
pytest>=8.0
extra-streamlit-components
# ffmpeg via pip, BUNDLED IN THE WHEEL. Streamlit Cloud cannot apt-install it (expired
# Debian 11 source in its image) and could not download one at runtime either
# (static-ffmpeg wrote into site-packages and needed github.com; it failed live).
# imageio-ffmpeg carries the binary inside the wheel: no download, no write, no network.
# It ships no ffprobe; durations come from `ffmpeg -i`.
imageio-ffmpeg>=0.6
